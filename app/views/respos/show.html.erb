<h1>Respos#show</h1>

<div class="card" style="width: fit-content;">
  <div class="card-body">
    <h5 class="card-title"><%= " #{@respo.nom} #{@respo.prenom} " %></h5>
    <p class="card-text">Email : <%= @respo.email %></p>
    <%= link_to "Editer profile", "/respos/#{@respo.id}/edit", class:"btn btn-primary" %>
    <%= link_to "Déconnexion", "/destroy", class:"btn btn-outline-danger" %>
  </div>
</div>