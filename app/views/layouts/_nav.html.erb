<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <%= link_to "Biblio", publics_url, class:'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
      <ul class="nav nav-pills mx-auto me-auto mb-2 mb-lg-0" id="v-pills-tab" role="tablist">
        <li class="nav-item">
          <%= link_to "Livres", '/documents', class:"nav-link py-2 mx-2 px-4 " + ( !request.path.starts_with?('/documents') ? "": "active") %>
        </li>
        <li class="nav-item">
          <%= link_to "Materiels", '/materiels', class:"nav-link py-2 mx-2 px-4 " + ( !request.path.starts_with?('/materiels') ? "": "active") %>
        </li>
        
        <% if !current_adherent.nil?%>
          <% if  current_adherent.admin  %>
            <li class="nav-item">
              <%= link_to "Adherents", '/adherents', class:"nav-link py-2 mx-2 px-4 " + ( request.path != '/adherents' ? "": "active") %>
            </li>
          <% end %>
      </ul>
      <%= link_to "Profile", current_adherent, class:"py-2 mx-2 px-4 btn btn-outline-success "%>
      <% end %>
    </div>
  </div>
</nav>